version: '3.8'

services:
  db:
    image: bitnami/postgresql:latest
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_USER=agenda
      - POSTGRES_PASSWORD=agenda
      - POSTGRES_DB=agenda
